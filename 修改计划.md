# 调整 smart-search MCP 的 Deep Research 触发与执行契约

## 摘要
- 对齐上游 Deep Research 契约：以 [README.zh-CN](https://github.com/konbakuyomu/smartsearch/blob/main/README.zh-CN.md) 和本机 `smart-search 0.1.12` 的 `smart-search deep --help` 为准，把 `deep` 视为“离线 planner”，不再在 `smart_deep_research` 内部隐式执行全部步骤。
- 这次主要修四个根因：工具描述不够像“深搜入口”、规划和执行混在一个工具里、返回结果太像“最终答案”导致模型提前收敛、参数契约已经过时（`budget=balanced`、`format` 默认值不一致、`<key-url>` 占位符未建模）。
- 保持兼容：保留现有 `smart_deep_research(query, budget, format)` 名称和主参数，但改成“生成计划并创建研究会话”；新增执行工具，不要求你重配 Cherry Studio 的基础接入。

## 接口与实现变更
- 在 `smart-search-mcp.js` 中把 `smart_deep_research` 改成 planner-only。
  - 描述改为：当用户提到“深度搜索 / 深度调研 / 核验 / 对比 / 选型 / 多来源 / serious review / deep research”时优先调用此工具，而不是 `smart_search`。
  - 输入改为 `budget: quick|standard|deep`，默认 `standard`；`format` 默认 `json`，因为后续执行要靠结构化字段驱动。
  - 返回统一 JSON：`research_id`、`status:"planned"`、`research_plan`、`pending_steps`、`next_action:"call smart_deep_execute"`、`not_final:true`。
- 新增 `smart_deep_execute` 工具，负责按会话执行 Deep Research。
  - 输入：`research_id` 必填；`max_steps` 默认 `1`；`selected_urls` 可选，用于给带 `<key-url>` 的 `fetch` 步骤补 URL；`format` 支持 `json|markdown`，默认 `json`。
  - 逻辑：每次只执行“下一个可执行步骤”；如果遇到 `<key-url>` 占位符且未提供 `selected_urls[step_id]`，返回 `status:"needs_input"`、`required_inputs` 和候选来源提示，不把该步骤标成完成。
  - 返回：`completed_steps`、`pending_steps`、`last_step_result`、`evidence_summary`、`next_action`、`ready_for_answer`。
- 在服务端保存轻量会话状态，不要求模型把整份 plan 原样回传。
  - 使用进程内 `Map` 存 `research_id -> { plan, progress, stepResults, createdAt }`。
  - `smart_deep_research` 创建会话；`smart_deep_execute` 读取/更新会话；MCP 进程结束即失效，不做磁盘持久化。
- 收紧普通工具与 Deep Research 的选择边界。
  - `smart_search` 描述改成“一次性快速搜索”；明确写出“不适合深度调研、核验、多步取证”。
  - `smart_doctor` 描述改成“仅配置/连通性预检，不是研究取证步骤”。
  - `smart_fetch`、`smart_exa_search`、`smart_map` 的描述补充“常作为 Deep Research 的后续步骤”。
- 修正执行器细节，避免“看起来执行了，实际上没取到证据”。
  - 用稳健 tokenizer 替换当前 `parseCommandArgs` 的手搓空格切分，正确处理引号、Windows 路径和 `--output` 剥离。
  - 校验 `plan.mode === "deep_research"`、`steps[].tool` 在白名单内，且 `doctor` 只能走 `preflight` 不能进入 `steps`。
  - 不再把 `plan.decomposition` 当字符串数组直接渲染；按对象字段展示 `id`、`question`、`reason`。
  - `runSmartSearch` 只在 `exit code === 0` 时判成功；有 stdout 但退出码非 0 也按失败处理。
- 同步文档。
  - 更新本地 `README.md` 中“自动执行所有步骤”的表述，改成“先规划，再按会话执行”。
  - 把 Deep Research 示例、`budget` 枚举、返回结构和推荐调度顺序写清楚，避免后续再被旧描述误导。

## 测试方案
- CLI 契约测试：
  - `smart-search deep --help` 显示 `--budget {quick,standard,deep}`，适配器 schema 与之完全一致。
  - `smart_deep_research` 返回 `status:"planned"`、`research_id`、`pending_steps`，且 `not_final:true`。
- 调度行为测试：
  - 输入“深度搜索一下最近的比特币行情”时，应先命中 `smart_deep_research`，而不是 `smart_search`。
  - 之后连续调用 `smart_deep_execute` 至少 2 轮，直到 `ready_for_answer:true` 或 `needs_input`，不能在首轮 plan 返回后看起来像最终完成。
- 占位符与阻塞测试：
  - 对当前 [plan.md](/D:/AI/smart-search-mcp-adapter/plan.md) 这种包含 `fetch "<key-url>"` 的计划，执行器必须返回 `needs_input`，而不是把该步跳过后仍输出“研究结果”。
- 回归测试：
  - `smart_search`、`smart_fetch`、`smart_map`、`smart_exa_search`、`smart_doctor` 现有单工具调用不退化。
  - Windows 下包含空格的 query、URL、`--output` 路径都能正确解析和转发。
- 文档一致性测试：
  - 本地 `README.md`、工具 schema、stderr 日志中的阶段命名统一使用 `planned / in_progress / needs_input / completed`。

## 假设与默认值
- 以本机已安装的 `smart-search 0.1.12` 为实现基线，不兼容旧的 `balanced` budget；如果需要兼容旧调用，可在适配器里把 `balanced` 映射到 `standard`，但对外文档只写 `standard`。
- 这次不新增真正的“最终报告生成”工具，最终答案仍由上层模型基于 `smart_deep_execute` 返回的证据来写。
- 会话状态先做内存态，优先解决“容易触发、不会提前收敛”的问题；等行为稳定后，再考虑磁盘持久化或断点恢复。
